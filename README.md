# ProjectTwo

This project supports hashing with either a HashMap or a TreeMap. The boolean, mode, in Table.java, Line 39 determines which is used.
A value of false uses a TreeMap and true uses a HashMap instead.
